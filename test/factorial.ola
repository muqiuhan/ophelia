program factorial;

function factorial(n : Integer) : Long;
  begin
    if n = 0 then
      factorial := 1
    else
      factorial := n * factorial(n - 1)
  end;

var n : Integer;

begin
  for n := 0 to 16 do
    writeln(n, '! = ', factorial(n));
end.